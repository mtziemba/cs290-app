

<h2>
  <%= @article.title %>
</h2>
<span id="post-image"><%= image_tag @article.photo.url, :class => 'materialboxed' %></span>

<p class="post-body">
  
  <%= @article.text %>
</p>
 <hr>
<h4>Recommendations</h4>
<%= render @article.comments %>
 
<h6>Add a recommendation:</h6>
<%= render 'comments/form' %>

<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_grid_path %>